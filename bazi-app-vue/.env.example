# 佩璇智慧命理分析平台 - 前端環境變數範本
# 請複製此檔案為 .env.local (本地開發) 或 .env.production (生產環境)

# API 配置
VITE_API_BASE_URL=http://localhost:3000/api/v1  # API 基礎 URL

# 功能開關
VITE_ENABLE_ANALYTICS=false                     # 是否啟用分析功能
VITE_ENABLE_PREMIUM_FEATURES=false              # 是否啟用高級功能
VITE_ENABLE_DEBUG_MODE=false                    # 是否啟用調試模式

# 界面配置
VITE_DEFAULT_LOCALE=zh-TW                       # 預設語言
VITE_AVAILABLE_LOCALES=zh-TW,en-US              # 可用語言列表
VITE_DEFAULT_THEME=light                        # 預設主題 (light/dark)

# 存儲配置
VITE_STORAGE_PREFIX=peixuan_                    # 本地存儲前綴
VITE_STORAGE_EXPIRY=604800                      # 本地存儲過期時間 (秒)

# 第三方服務 (如有需要)
VITE_GEOCODE_API_KEY=                           # 地理編碼 API 密鑰
VITE_TIMEZONE_API_URL=                          # 時區 API URL