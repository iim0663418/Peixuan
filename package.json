{
  "name": "peixuan-api",
  "version": "2.0.0",
  "description": "Chinese astrology calculation API for LLMs — BaZi + Zi Wei Dou Shu",
  "main": "src/index.ts",
  "scripts": {
    "build": "wrangler deploy --dry-run --outdir=dist",
    "deploy": "wrangler deploy",
    "deploy:staging": "wrangler deploy --env staging",
    "test": "vitest run",
    "lint": "eslint src/"
  },
  "dependencies": {
    "date-fns": "^4.1.0",
    "itty-router": "^5.0.0",
    "lunar-typescript": "^1.8.0"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.8.0",
    "@cloudflare/workers-types": "^4.0.0",
    "eslint": "^9.0.0",
    "typescript": "^5.8.0",
    "vitest": "^3.0.0"
  },
  "license": "CC-BY-NC-SA-4.0"
}
