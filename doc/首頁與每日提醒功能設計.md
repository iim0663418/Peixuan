# 首頁與每日提醒功能設計

**文件版本**: v1.0  
**建立日期**: 2025-12-06  
**設計者**: Amazon Q Developer  
**狀態**: 待實作

---

## 📋 功能概述

### 背景
當前專案缺少實質意義的首頁（index），用戶直接進入命盤計算頁面，缺乏服務導覽與功能介紹。同時，基於「Solution A: Lightweight Daily Reminder」方案，需要新增輕量化每日運勢提醒功能。

### 目標
1. 建立統合整個服務的首頁，提供清晰的服務導覽
2. 實作每日運勢提醒功能（模板化，無 AI 生成，成本 $0）
3. 優化用戶體驗，降低認知負擔
4. 保持架構一致性，符合現有設計系統

### 核心價值
- **統一入口**：首頁作為服務導覽中心
- **降低門檻**：自動檢測 localStorage，無需記憶 UUID
- **輕量提醒**：每日運勢提醒，快速掌握吉凶
- **成本控制**：模板化輸出，無 AI 成本

---

## 👤 用戶故事

### US-1: 首次訪問用戶
**作為** 首次訪問的用戶  
**我想要** 快速了解平台提供哪些服務  
**以便** 選擇適合我的功能開始使用

**驗收標準**：
- ✅ 首頁清晰展示 4 大核心服務
- ✅ 每個服務有圖標、標題、描述
- ✅ 提供「開始計算命盤」按鈕
- ✅ 特色說明使用白話文，避免技術術語

### US-2: 回訪用戶
**作為** 已計算過命盤的回訪用戶  
**我想要** 快速查看我的命盤分析  
**以便** 無需重新輸入資料

**驗收標準**：
- ✅ 自動檢測 localStorage 中的命盤資料
- ✅ 顯示「您有已保存的命盤」提示
- ✅ 提供「繼續查看命盤」按鈕
- ✅ 一鍵進入結果頁面

### US-3: 每日運勢查詢
**作為** 已有命盤的用戶  
**我想要** 快速查看今日運勢提醒  
**以便** 了解今日吉凶，做好準備

**驗收標準**：
- ✅ 提供日期選擇器（預設今天）
- ✅ 顯示 1-2 句輕量化提醒
- ✅ 顯示吉凶標籤（favorable/unfavorable）
- ✅ 快取命中 <50ms
- ✅ 無需命盤時引導前往計算

---

## 🗺️ 路由架構

### 新路由設計

| 路徑 | 名稱 | 組件 | 說明 |
|------|------|------|------|
| `/` | home | HomeView | 首頁（服務導覽） |
| `/calculate` | calculate | UnifiedView | 整合命盤計算 |
| `/personality` | personality | AIAnalysisView | 佩璇性格分析 |
| `/fortune` | fortune | AdvancedAnalysisView | 佩璇運勢分析 |
| `/daily` | daily | DailyReminderView | 每日運勢提醒 |

### 向後兼容（Redirect）

| 舊路徑 | 新路徑 |
|--------|--------|
| `/unified` | `/calculate` |
| `/ai-analysis` | `/personality` |
| `/advanced-analysis` | `/fortune` |

### 路由配置範例

```typescript
// router/index.ts
import { createRouter, createWebHistory } from 'vue-router';

const routes = [
  {
    path: '/',
    name: 'home',
    component: () => import('@/views/HomeView.vue'),
    meta: { title: '首頁 - 佩璇' }
  },
  {
    path: '/calculate',
    name: 'calculate',
    component: () => import('@/views/UnifiedView.vue'),
    meta: { title: '整合命盤計算 - 佩璇' }
  },
  {
    path: '/personality',
    name: 'personality',
    component: () => import('@/views/AIAnalysisView.vue'),
    meta: { title: '佩璇性格分析 - 佩璇' }
  },
  {
    path: '/fortune',
    name: 'fortune',
    component: () => import('@/views/AdvancedAnalysisView.vue'),
    meta: { title: '佩璇運勢分析 - 佩璇' }
  },
  {
    path: '/daily',
    name: 'daily',
    component: () => import('@/views/DailyReminderView.vue'),
    meta: { title: '每日運勢提醒 - 佩璇' }
  },
  // 向後兼容
  { path: '/unified', redirect: '/calculate' },
  { path: '/ai-analysis', redirect: '/personality' },
  { path: '/advanced-analysis', redirect: '/fortune' }
];

const router = createRouter({
  history: createWebHistory(),
  routes,
  scrollBehavior(to, from, savedPosition) {
    return savedPosition || { top: 0 };
  }
});

// 動態更新頁面標題
router.beforeEach((to, from, next) => {
  document.title = to.meta.title as string || '佩璇';
  next();
});

export default router;
```

---

## 🧩 組件設計

### 1. HomeView.vue（首頁主體）

**職責**：
- 展示平台價值主張（Hero Section）
- 提供服務導覽（4 個服務卡片）
- 快速入口（localStorage 檢測）
- 特色說明（白話版）

**狀態管理**：
```typescript
const hasChart = computed(() => !!chartStore.currentChartId);
```

**核心邏輯**：
- 檢測 `chartStore.currentChartId` 是否存在
- 有命盤 → 顯示「繼續查看命盤」
- 沒命盤 → 顯示「開始計算命盤」
- 都導向 `/calculate`（UnifiedView 自動處理）

**區塊結構**：
1. Hero Section（主視覺）
2. Services Section（服務卡片 Grid）
3. Quick Access Section（快速入口）
4. Features Section（特色說明）

---

### 2. ServiceCard.vue（服務卡片）

**職責**：
- 可復用的服務卡片組件
- 支援圖標、標題、描述、路由、顏色、標籤

**Props**：
```typescript
interface ServiceCardProps {
  service: {
    id: string;
    icon: string;
    title: string;
    desc: string;
    route: string;
    color: 'primary' | 'success' | 'warning' | 'info';
    badge?: string; // 例如 'NEW'
  }
}
```

**互動**：
- 點擊卡片 → 導航至對應路由
- Hover 動畫（translateY + shadow）
- 響應式設計（移動端優化）

---

### 3. DailyReminderView.vue（每日提醒頁面）

**職責**：
- 每日運勢提醒的獨立頁面
- 檢測是否有命盤
- 無命盤時引導前往計算

**核心邏輯**：
```typescript
const currentChartId = computed(() => chartStore.currentChartId);

// 無命盤時顯示 Empty State
if (!currentChartId.value) {
  // 顯示 el-empty + 「前往計算命盤」按鈕
}
```

---

### 4. DailyReminderCard.vue（提醒卡片）

**職責**：
- 顯示每日運勢提醒（1-2 句）
- 日期選擇器（預設今天）
- 快取狀態提示
- 吉凶標籤

**Props**：
```typescript
interface DailyReminderCardProps {
  chartId: string;
}
```

**狀態管理**：
```typescript
const selectedDate = ref(new Date());
const reminderText = ref('');
const tags = ref<Array<{ label: string; type: string }>>([]);
const loading = ref(false);
const cached = ref(false);
```

**API 調用**：
```typescript
const loadReminder = async () => {
  loading.value = true;
  try {
    const result = await getDailyReminder(chartId, selectedDate.value);
    reminderText.value = result.text;
    tags.value = result.tags;
    cached.value = result.cached;
  } catch (error) {
    // Fallback: 顯示通用吉祥話
    reminderText.value = '今日平安順遂，保持平常心 ✨';
    tags.value = [];
  } finally {
    loading.value = false;
  }
};
```

**快取策略**：
- Key: `daily-reminder-${chartId}-${dateKey}`
- Storage: localStorage
- TTL: 永久（日期固定，結果不變）

---

## 🔌 API 整合

### 新增端點：GET /api/v1/daily-reminder

**請求參數**：
```typescript
{
  chartId: string;  // 命盤 ID
  date: string;     // ISO 8601 格式（例如：2025-12-06T00:00:00.000Z）
}
```

**回應格式**：
```typescript
{
  text: string;  // 1-2 句提醒文字
  tags: Array<{
    label: string;  // 標籤文字（例如：「宜靜」）
    type: 'success' | 'warning' | 'info';  // 標籤類型
  }>;
  cached: boolean;  // 是否來自快取
}
```

**範例回應**：
```json
{
  "text": "今日宜靜不宜動，保持平常心即可 ✨",
  "tags": [
    { "label": "宜靜", "type": "success" },
    { "label": "平安", "type": "info" }
  ],
  "cached": false
}
```

### 前端服務層

```typescript
// src/services/unifiedApiService.ts

export interface DailyReminder {
  text: string;
  tags: Array<{ label: string; type: 'success' | 'warning' | 'info' }>;
  cached: boolean;
}

export async function getDailyReminder(
  chartId: string, 
  date: Date
): Promise<DailyReminder> {
  const dateKey = date.toISOString().split('T')[0];
  const cacheKey = `daily-reminder-${chartId}-${dateKey}`;

  // 先查 localStorage（永久快取）
  const cached = localStorage.getItem(cacheKey);
  if (cached) {
    return { ...JSON.parse(cached), cached: true };
  }

  // API 調用
  const response = await axios.get<DailyReminder>(
    `/api/v1/daily-reminder`,
    { params: { chartId, date: date.toISOString() } }
  );

  // 永久快取（日期固定，結果不變）
  localStorage.setItem(cacheKey, JSON.stringify(response.data));
  
  return { ...response.data, cached: false };
}
```

---

## 📦 狀態管理

### chartStore（Pinia）

**現有狀態**：
```typescript
{
  currentChartId: string | null;
  currentChart: ChartData | null;
}
```

**使用方式**：
```typescript
import { useChartStore } from '@/stores/chartStore';

const chartStore = useChartStore();
const hasChart = computed(() => !!chartStore.currentChartId);
```

**localStorage 同步**：
- chartStore 已實現 localStorage 自動同步
- 無需額外實作

---

## 🎨 樣式規範

### 設計系統變數（design-tokens.css）

**使用原則**：
- 完整使用 CSS 變數
- 避免硬編碼數值
- 保持視覺一致性

**常用變數**：
```css
/* 間距 */
--space-xs: 0.25rem;
--space-sm: 0.5rem;
--space-md: 1rem;
--space-lg: 1.5rem;
--space-xl: 2rem;
--space-2xl: 3rem;
--space-3xl: 4rem;

/* 字體大小 */
--font-size-xs: 0.75rem;
--font-size-sm: 0.875rem;
--font-size-md: 1rem;
--font-size-lg: 1.125rem;
--font-size-xl: 1.25rem;
--font-size-2xl: 1.5rem;
--font-size-3xl: 2rem;

/* 圓角 */
--radius-sm: 0.25rem;
--radius-md: 0.5rem;
--radius-lg: 1rem;

/* 陰影 */
--shadow-sm: 0 1px 2px rgba(0, 0, 0, 0.05);
--shadow-md: 0 4px 6px rgba(0, 0, 0, 0.1);
--shadow-lg: 0 10px 15px rgba(0, 0, 0, 0.1);

/* 顏色 */
--bg-primary: #f7f8fa;
--bg-secondary: #ffffff;
--bg-card: #ffffff;
--text-primary: #303133;
--text-secondary: #606266;
```

### 響應式斷點

```css
/* 移動版 */
@media (max-width: 767px) {
  .service-grid {
    grid-template-columns: 1fr;
  }
}

/* 平板版 */
@media (max-width: 1023px) {
  .service-grid {
    grid-template-columns: repeat(2, 1fr);
  }
}

/* 桌面版 */
@media (min-width: 1024px) {
  .service-grid {
    grid-template-columns: repeat(4, 1fr);
  }
}
```

---

## 📝 特色說明（白話版）

**原版（技術性）**：
- 🔄 多術數交互驗證：八字與紫微斗數交叉驗證，提升分析可信度
- 🤖 AI 智能分析：Gemini 2.5 Flash 驅動，提供敘事化深度解讀
- ⚡ 快速響應：快取命中 0.118s，180x 提速，極致體驗

**新版（白話版）**：
- 💯 分析更準確：八字與紫微斗數雙重驗證，結果更可靠
- 💬 解讀更易懂：AI 幫你把複雜的命理轉成白話文
- ⚡ 隨時能查看：保存你的命盤，隨時回來看都很快

**設計原則**：
- 避免技術術語（快取、API、Token）
- 使用用戶視角（準確、易懂、便利）
- 強調實際價值（可靠、白話、快速）

---

## 🚀 實作順序

### Phase 1: 路由與基礎架構（3-4h）

**任務**：
1. 更新 `router/index.ts`（新增路由 + 向後兼容）
2. 建立 `HomeView.vue` 骨架
3. 建立 `DailyReminderView.vue` 骨架
4. 更新 `App.vue` Navbar（新增首頁 + 每日提醒連結）

**驗收標準**：
- ✅ 所有路由可正常訪問
- ✅ 舊路由自動 redirect
- ✅ Navbar 顯示新連結
- ✅ 頁面標題動態更新

---

### Phase 2: 首頁實作（4-5h）

**任務**：
1. 實作 `HomeView.vue` 完整內容
   - Hero Section
   - Services Section
   - Quick Access Section（localStorage 檢測）
   - Features Section（白話版）
2. 建立 `ServiceCard.vue` 可復用組件
3. 樣式實作（響應式 Grid + 設計系統變數）

**驗收標準**：
- ✅ 首頁完整顯示 4 個區塊
- ✅ 服務卡片可點擊導航
- ✅ 快速入口正確檢測 localStorage
- ✅ 響應式佈局（桌面 4 欄、平板 2 欄、移動 1 欄）
- ✅ 特色說明使用白話文

---

### Phase 3: 每日提醒實作（4-5h）

**任務**：
1. 實作 `DailyReminderCard.vue`
   - 日期選擇器
   - API 調用
   - 快取邏輯
   - Loading 狀態
   - 錯誤處理
2. 完善 `DailyReminderView.vue`
   - 無命盤時的 Empty State
   - 引導按鈕
3. 新增 `getDailyReminder()` API 服務層
4. 樣式實作

**驗收標準**：
- ✅ 日期選擇器正常運作
- ✅ API 調用成功
- ✅ 快取命中 <50ms
- ✅ 無命盤時顯示引導
- ✅ 錯誤時顯示 fallback 文案

---

## ⏱️ 工時估算

| 階段 | 任務 | 預估時間 |
|------|------|---------|
| **Phase 1** | 路由與基礎架構 | 3-4h |
| | - 路由配置 | 1h |
| | - HomeView 骨架 | 1h |
| | - DailyReminderView 骨架 | 0.5h |
| | - Navbar 更新 | 0.5-1h |
| **Phase 2** | 首頁實作 | 4-5h |
| | - HomeView 完整內容 | 2h |
| | - ServiceCard 組件 | 1h |
| | - 樣式與響應式 | 1-2h |
| **Phase 3** | 每日提醒實作 | 4-5h |
| | - DailyReminderCard 組件 | 2h |
| | - DailyReminderView 完善 | 1h |
| | - API 服務層 | 1h |
| | - 樣式與測試 | 0.5-1h |
| **總計** | | **11-14h** |

---

## ✅ 驗收標準

### 功能驗收

- [ ] 首頁正確顯示 4 個服務卡片
- [ ] 快速入口正確檢測 localStorage 狀態
- [ ] 有命盤時顯示「繼續查看命盤」
- [ ] 沒命盤時顯示「開始計算命盤」
- [ ] 服務卡片可點擊導航至對應頁面
- [ ] 每日提醒頁面正常運作
- [ ] 日期選擇器可正常切換日期
- [ ] 每日提醒顯示 1-2 句文字 + 標籤
- [ ] 無命盤時顯示引導按鈕
- [ ] 所有路由可正常訪問
- [ ] 舊路由自動 redirect

### 性能驗收

- [ ] 快取命中響應時間 <50ms
- [ ] 首頁載入時間 <1s
- [ ] 圖片/資源優化（無大型資源）
- [ ] localStorage 讀寫正常

### UX 驗收

- [ ] 響應式佈局正常（桌面/平板/移動）
- [ ] 觸控目標至少 44x44px
- [ ] Hover 動畫流暢
- [ ] Loading 狀態清晰
- [ ] 錯誤提示友善
- [ ] 特色說明使用白話文

### 程式碼品質

- [ ] 完整使用 TypeScript
- [ ] 完整使用設計系統變數
- [ ] 無 ESLint 錯誤
- [ ] 組件可復用
- [ ] 程式碼註解清晰

---

## 📚 參考資料

- [Solution A: Lightweight Daily Reminder](../progress.md)
- [設計系統](../bazi-app-vue/src/assets/design-tokens.css)
- [chartStore](../bazi-app-vue/src/stores/chartStore.ts)
- [unifiedApiService](../bazi-app-vue/src/services/unifiedApiService.ts)

---

**文件結束**
