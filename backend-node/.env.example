# 佩璇智能命理分析平台 - 環境變數範本
# 請複製此檔案為 .env.dev (開發環境) 或 .env.prod (生產環境)

# 基本配置
NODE_ENV=development    # 環境類型 (development/test/production)
PORT=3000               # API 服務埠

# 資料庫配置
DB_HOST=postgres        # PostgreSQL 主機名
DB_PORT=5432            # PostgreSQL 埠
DB_USERNAME=postgres    # 資料庫用戶名
DB_PASSWORD=devpassword # 資料庫密碼 (生產環境請更改)
DB_NAME=peixuan_dev     # 資料庫名稱

# Redis 配置
REDIS_HOST=redis        # Redis 主機名
REDIS_PORT=6379         # Redis 埠
REDIS_PASSWORD=         # Redis 密碼 (生產環境請設定)

# JWT 配置
JWT_SECRET=dev-jwt-secret-change-in-production  # JWT 密鑰 (生產環境請使用強密碼)
JWT_EXPIRES_IN=24h                              # Token 有效期
JWT_REFRESH_EXPIRES_IN=7d                       # 刷新 Token 有效期

# API 配置
API_RATE_LIMIT=1000           # API 請求限制 (每分鐘)
CALCULATION_RATE_LIMIT=100    # 計算服務請求限制 (每分鐘)

# 日誌配置
LOG_LEVEL=info                # 日誌級別 (debug/info/warn/error)
LOG_FILE_PATH=logs/app.log    # 日誌文件路徑

# 安全配置
CORS_ORIGIN=*                 # CORS 來源 (生產環境請指定確切域名)
ENABLE_API_DOCS=true          # 是否啟用 API 文檔 (生產環境建議關閉)

# 功能開關
ENABLE_PREMIUM_FEATURES=false # 是否啟用高級功能
TEST_MODE=false               # 是否為測試模式

# 監控配置
ENABLE_METRICS=true           # 是否啟用指標收集
METRICS_PATH=/metrics         # 指標路徑